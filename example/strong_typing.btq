fn greet(argc:int, argv:string):string => "Hello, world!"

fn chisnall():int {
    int b = 12;
    ret b;
}

fn identity(x:int):int => x

fn main(argc:int, argv:string):int {
    printf("Received %d args", argc);
    string msg = greet(420, "blaze it");
    printf("%d blaze it", 420);
    puts(msg);
    ret 0;
}